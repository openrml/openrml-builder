# RML — Role Markup Language

> **Референсная реализация** · v0.1.0

[English](./README.md) · **Русский** · [Українська](./README.uk.md)

---

## Видение

AI-ассистенты стали повсеместными. Но способ описания ролей для них по-прежнему остаётся полностью неформальным — разрозненные промпты, скопированные инструкции, недокументированные соглашения. Нет стандарта. Нет общего словаря. Нет способа валидировать, передавать или развивать описание роли между инструментами, командами и временем.

**RML создан, чтобы это исправить.**

Role Markup Language — структурированная, читаемая человеком спецификация для описания личности AI-ассистента, его поведения, области экспертизы и паттернов взаимодействия. Она рассматривает описания ролей как полноценные артефакты — версионируемые, валидируемые, экспортируемые и интероперабельные.

Через 3–5 лет мы видим мир, где:
- Любая AI-платформа принимает `.rml`-описание роли без перевода
- Авторы ролей публикуют, делятся и форкают роли как код
- Инструменты соответствия проверяют, что поведение AI совпадает с его объявленной ролью
- Организации ведут аудируемые реестры ролей для всего AI-стека

Этот репозиторий — **референсная реализация** этого будущего.

---

## Что такое этот репозиторий

```
Этот репозиторий — референсная реализация RML 1.0.
```

Это не шаблон. Это не boilerplate. Это каноническая демонстрация того, как RML-роли создаются, валидируются, экспортируются и используются.

Всё здесь — доменная модель, формат экспорта, система идентичности, конструктор UI — существует для того, чтобы доказать: спецификация полная, согласованная и реализуемая.

Будущие реализации (CLI-инструменты, расширения VS Code, API-валидаторы, нативные мобильные приложения) будут измеряться тем, что определено здесь.

---

## Архитектура

RML построен на строгом разделении ответственности:

```
src/
├── core/
│   ├── domain/role/          # Канонический тип Role и перечисления
│   ├── rml/                  # Парсер и экспортер — граница спецификации
│   └── services/             # Хеширование идентичности, логика лицензий
│
├── application/
│   └── use-cases/            # Бизнес-операции: создать, сохранить, экспортировать, импортировать
│
├── infrastructure/
│   ├── storage/              # Адаптеры: IndexedDB, localStorage, session
│   └── template/             # Загрузка и предпросмотр шаблонов
│
├── data/
│   ├── templates/            # Кураторские шаблоны ролей по категориям
│   └── template-previews.ts  # Слой обнаружения
│
└── presentation/
    ├── components/           # Конструктор UI, шаги, живой предпросмотр
    └── contexts/             # Границы состояния React
```

**Ключевые архитектурные принципы:**

- **Слой `core/` не имеет зависимостей от UI.** Его можно извлечь и использовать в любой среде выполнения.
- **Экспортер `rml/` — граница спецификации.** Текстовый формат, который он производит, И ЕСТЬ стандарт RML.
- **Идентичность детерминирована.** Одно и то же содержимое роли всегда даёт один и тот же хеш RML Identity.
- **Хранилище подключаемо.** Составной адаптер выбирает наилучшую доступную стратегию хранения.

Подробные диаграммы и записи архитектурных решений — в [`architecture/`](./architecture/).

---

## Приватность и данные

Ваши роли никогда не покидают устройство. Аккаунт не нужен, никакой сервер не получает ваши данные, телеметрия не собирается. Единственное хранилище — IndexedDB вашего браузера, локальная база данных, которая ничего никуда не передаёт.

Это осознанное архитектурное решение, а не ограничение. Роли, созданные в RolesAI, часто содержат личный материал: цели, трудности, контекст ментального здоровья, семейную динамику, карьерные тревоги. Такие данные принадлежат тому, кто их создал — и только ему.

**Что и где хранится:**

| Данные | Хранилище | Покидают устройство? |
|--------|-----------|----------------------|
| Ваши роли | IndexedDB (браузер) | Никогда |
| Настройки UI (язык, тема) | localStorage (браузер) | Никогда |
| Кеш шаблонов | sessionStorage (очищается при закрытии вкладки) | Никогда |
| Экспортированные `.rml.txt` файлы | Ваша файловая система | Только если вы ими поделитесь |

**Экспорт — это ваш бэкап.** Синхронизации с облаком нет, восстановления аккаунта нет, «восстановить с сервера» нет. Если вы очистите данные браузера, роли исчезнут. Экспортируйте регулярно — каждый `.rml.txt` файл это полная самодостаточная резервная копия роли.

**PWA не меняет эту модель.** Установка RolesAI как приложения на экран телефона не создаёт аккаунт и не включает серверное взаимодействие. Установленное приложение использует то же локальное хранилище браузера, что и веб-версия.

Полный разбор — в [`docs/privacy.md`](./docs/privacy.md).

---

## Формат RML

Роль, экспортированная через RML, выглядит так:

```
RML 1.0.0 — Frontend Dev Pro [published]
IDENTITY: RML/CT/1.0.0/3d0b/4327/5287/baf3/4186/e3c9/ba9c/aba2
REFERENCE: rml://anonymous/frontend-dev-pro/1.0.0
ARCHETYPE: creator
CATEGORY: technology

📋 STEP 1: BASE INFORMATION
Role Name: Frontend Dev Pro
...

🎨 STEP 2: VISUAL PORTRAIT
...

💬 STEP 3: BEHAVIOR & TONE
...

🎯 STEP 4: EXPERTISE & RULES
...

🗺️ STEP 5: JOURNEY SESSIONS
...

👥 STEP 6: TEAM COLLABORATION
...

🧠 STEP 7: MEMORY SYSTEM
...

⚖️ STEP 8: ETHICS & VERSIONS
...
```

Формат намеренно читаем человеком и одновременно разбираем языковой моделью. Полная спецификация — в [`spec/`](./spec/).

---

## Дорожная карта

### 0.1.0 — Начальная референсная реализация ✅
- Доменная модель (`Role`, `Session`, `EthicalRule`, `RMLIdentity`)
- 8-шаговый конструктор UI
- Текстовый экспорт с детерминированным хешированием идентичности
- Импорт и повторный экспорт без потерь
- Библиотека шаблонов (8 категорий, 37+ шаблонов)
- Подключаемые адаптеры хранилища

### 0.2.0 — Укрепление спецификации
- [ ] Формальная JSON Schema для типа Role
- [ ] Версионирование формата экспорта (`RML-Format: 1.0`)
- [ ] Слой валидации со структурированными сообщениями об ошибках
- [ ] Тесты точности обратного преобразования (экспорт → импорт → экспорт = идентично)

### 0.3.0 — Система расширений
- [ ] Интерфейс плагинов для пользовательских типов шагов
- [ ] Система хуков для трансформаций до/после экспорта
- [ ] Именование пользовательских полей (`x-myorg-field`)

### 1.0.0 — Выравнивание со стабильной спецификацией
- [ ] Набор тестов соответствия спецификации
- [ ] Референсный парсер на втором языке (Python или Go)
- [ ] Дизайн API публичного реестра ролей
- [ ] Формальная политика устаревания

### После 1.0
- Несколько независимых реализаций
- Экосистема внешних контрибьюторов
- Система бейджей соответствия спецификации
- Интеграции с экосистемой (адаптеры OpenAI, Claude, Gemini)

---

## Быстрый старт

```bash
npm install
npm run dev       # Сервер разработки на localhost:3000
npm run build     # Продакшн-сборка
npm run preview   # Предпросмотр продакшн-сборки
```

**Требования:** Node.js 18+, npm 9+

---

## Структура проекта

```
/src              — Исходный код референсной реализации
/spec             — Документы спецификации формата RML
/docs             — Руководства, концепции, заметки по интеграции
/architecture     — ADR и системные диаграммы
/examples         — Примеры .rml-описаний ролей
```

---

## Участие в разработке

См. [CONTRIBUTING.md](./CONTRIBUTING.md). Все вклады должны соответствовать принципам, определённым в [FOUNDATION.md](./FOUNDATION.md).

---

## Лицензия

MIT — см. [LICENSE](./LICENSE)

---

*RML — открытая спецификация. Эта реализация поддерживается как каноническая референсная.*
